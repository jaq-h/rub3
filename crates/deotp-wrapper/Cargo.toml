[package]
name = "deotp-wrapper"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "deotp-wrapper"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }

[target.'cfg(unix)'.dependencies]
nix = { version = "0.29", features = ["signal", "process"] }
libc = "0.2"
